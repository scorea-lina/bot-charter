# Project wiring

Each project should add a `docs/INDEX.md` that references this charter.

- Global protocol lives here.
- Project-specific overrides (channels, URLs, secrets locations) live in the project repo.
